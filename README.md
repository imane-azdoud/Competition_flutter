# Competition_flutter

This is my Flutter project for the Competition exam.

It contains the full source code, including all Dart files in the `lib` folder.

---

## Getting Started

This project was created with Flutter.

To run the app, make sure you have Flutter installed, then run:

```bash
flutter pub get
flutter run